---
import { Icon } from 'astro-icon/components';
import ThemeToggle from './ThemeToggle.astro';

const textLinks: { label: string; href: string }[] = [
	{ label: 'Home', href: '/' },
	{ label: 'Projects', href: '/projects/' },
	{ label: 'About', href: '/about/' },
];

const iconLinks: { label: string; href: string; icon: string }[] = [
	{ label: 'Email', href: 'mailto:contact@pd-portfolio.net', icon: 'lucide:mail' },
	{ label: 'GitHub', href: 'https://github.com/Paul1404', icon: 'lucide:github' },
];

const path = Astro.url.pathname;
const isActive = (href: string) =>
	href === '/' ? path === '/' : path.startsWith(href);
---

<header
	class="sticky top-0 z-50 border-b border-line bg-canvas/80 backdrop-blur-md"
>
	<nav class="mx-auto flex h-16 max-w-6xl items-center justify-between px-6">
		<a href="/" class="font-display text-lg font-bold tracking-tight">
			<span class="text-gradient">Paul Dresch</span>
		</a>

		<!-- Desktop -->
		<div class="hidden items-center gap-1 md:flex">
			{
				textLinks.map(({ label, href }) => (
					<a
						href={href}
						aria-current={isActive(href) ? 'page' : undefined}
						class:list={[
							'rounded-full px-3 py-1.5 text-sm transition-colors',
							isActive(href)
								? 'bg-brand-tint text-ink'
								: 'text-muted hover:text-ink',
						]}
					>
						{label}
					</a>
				))
			}
			<span class="mx-2 h-5 w-px bg-line"></span>
			{
				iconLinks.map(({ href, icon, label }) => (
					<a
						href={href}
						target={href.startsWith('http') ? '_blank' : undefined}
						rel={href.startsWith('http') ? 'noopener noreferrer' : undefined}
						class="inline-flex h-9 w-9 items-center justify-center rounded-full text-muted transition-colors hover:text-ink"
					>
						<span class="sr-only">{label}</span>
						<Icon name={icon} class="h-4 w-4" />
					</a>
				))
			}
			<ThemeToggle />
		</div>

		<!-- Mobile -->
		<details class="group relative md:hidden">
			<summary
				class="flex h-9 w-9 cursor-pointer list-none items-center justify-center rounded-full border border-line text-muted [&::-webkit-details-marker]:hidden"
			>
				<span class="sr-only">Menu</span>
				<Icon name="lucide:menu" class="h-5 w-5 group-open:hidden" />
				<Icon name="lucide:x" class="hidden h-5 w-5 group-open:block" />
			</summary>
			<div
				class="absolute right-0 mt-3 w-56 rounded-card border border-line bg-surface p-2 shadow-lg"
			>
				{
					textLinks.map(({ label, href }) => (
						<a
							href={href}
							class:list={[
								'block rounded-lg px-3 py-2 text-sm transition-colors',
								isActive(href) ? 'bg-brand-tint text-ink' : 'text-muted hover:bg-elevated hover:text-ink',
							]}
						>
							{label}
						</a>
					))
				}
				<div class="my-2 h-px bg-line"></div>
				<div class="flex items-center justify-between px-2">
					<div class="flex items-center gap-1">
						{
							iconLinks.map(({ href, icon, label }) => (
								<a
									href={href}
									target={href.startsWith('http') ? '_blank' : undefined}
									rel={href.startsWith('http') ? 'noopener noreferrer' : undefined}
									class="inline-flex h-9 w-9 items-center justify-center rounded-full text-muted transition-colors hover:text-ink"
								>
									<span class="sr-only">{label}</span>
									<Icon name={icon} class="h-4 w-4" />
								</a>
							))
						}
					</div>
					<ThemeToggle />
				</div>
			</div>
		</details>
	</nav>
</header>
